# Tool configs that own their own formatting. Biome and prettier disagree on
# JSONC trailing commas, so whichever formatter does not own the file must skip
# it — otherwise the two rewrite each other on every commit.
biome.jsonc
.markdownlint.jsonc

# Generated by righthook.
.righthook/
.github/workflows/righthook.yml
.github/dependabot.yml
lefthook.yml
presets/
workflows/
dist/
.test-build/
